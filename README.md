# Nimbis

A Redis-compatible database built with Rust, using object storage as the backend.

## Project Structure

- `crates/nimbis-resp` - RESP protocol implementation
- `crates/nimbis-server` - Nimbis server